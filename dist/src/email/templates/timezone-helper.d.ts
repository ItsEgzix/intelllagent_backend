export declare function convertTimezone(date: string, time: string, fromTimezone: string, toTimezone: string): {
    date: string;
    time: string;
    timezone: string;
};
export declare function calculateKLTime(date: string, time: string, timezone: string): string;
